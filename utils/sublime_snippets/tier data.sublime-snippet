<snippet><content><![CDATA[tier data=${1}]]></content>    <description> /Sets the data that will overwrite the input data for tier projects. Just if the “tier data” is empty it will use the one that the user set.Example: tier data=%tier_title%If the project is a tier project, it will not use the input data, but instead fill it from the variable %tier_data% which can e.g. be a extract-variable that downloads the URL and extracts the &amp;lt;title&amp;gt;&amp;lt;/title&amp;gt; value.</description>    <tabTrigger>tier data</tabTrigger></snippet>